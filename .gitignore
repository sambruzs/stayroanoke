node_modules
dist
.env
.DS_Store
*.local

# Local Netlify folder
.netlify

# Cached API tokens — should never be committed
.guesty-token.json

# Sub-account credentials for webhook registration script — never commit
.guesty-subaccounts.json
